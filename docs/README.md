## Offie documentation

Entry point for Offie docs: quick links and command reference.

---

## Quick links

- **Getting started**: [`getting-started.md`](getting-started.md) – install Offie, learn the CLI, and run the example workflow.
- **Commands**: see grouped command reference under [`commands/`](commands/).
- **Contributing commands**: [`contributing-commands.md`](contributing-commands.md) – how to add new commands with tests and docs.

---

## Commands overview

Offie commands are grouped by purpose.

- **Control flow**
  `if`, `while`, `for_each`, `for` – see [`commands/control-flow.md`](commands/control-flow.md).

- **Variables and data**
  `set_variable` – see [`commands/variables.md`](commands/variables.md).

- **Output**
  `print` – see [`commands/output.md`](commands/output.md).

As new command groups are added, create a new file under `commands/` and link it here.
